<div class="row grade">
  <div class="col-md-8">
    <h2>
      <%= link_to student.user.full_name, student_path(student.id) %>
      <small>Will Add Parents Here</small>
    </h2>
  </div>
  <div class="col-md-4">
    <%= link_to "Delete Post", student_path(student.id), method: :delete, class: 'btn btn-sm btn-danger' %>
  </div>
</div>
